<div class="container">
  <p id="notice"><%= notice %></p>


    <div class="row">
      <div class=" col-md-12 nombretienda">
        <h1><%= @store.name %></h1>
      </div>

        <!-- <% @store.products.each do |product| %>
          <div class="card-deck my-3">
            <div class="card">
              <div class="card-body">
                <h5 class="card-title"><%= product.name.capitalize! %></h5>
                <img class="card-img-top"><%= image_tag product.image.to_s, class:"rounded", style: 'height:25%;width:auto'%></img>
                <p class="card-text"><%= product.description.capitalize! %></p>
                <p class="card-text"><small class="text-muted"><%= product.price %></small></p>
                <p class="card-text"><small class="text-muted"><%= link_to 'Agregar Bandeja', store_product_orders_path(@store, product), class: 'btn btn-success btn-block', method: :post %></small></p>
              </div>
            </div>
          </div>
          <% end %> -->


      <div class=" col-md-12 tituloproductos">
        <h3><strong>Products:</strong></h3>
      </div>
    </div>
</div>
<div class="container">
  <div class="row">
      <% @store.products.each do |product| %>
      <div class=" col-sm-12 col-lg-3 float-left">
        <div class="card-deck">
          <div class="card">
            <div class="card-body">
              <img class="card-img-top rounded"><%= image_tag product.image.to_s, style: "max-width: 100%", class: "rounded" %></img>
              <h5 class="card-title"><%= product.name %></h5>
              <p class="card-text"><%= product.description%></p>
              <p class="card-text"><small class="text-muted"><%= product.price %></small></p>
              <p class="card-text"><%= link_to 'Agregar ', store_product_orders_path(@store, product), class: 'btn btn-danger float-right', method: :post %></p>
            </div>
          </div>
        </div>
      </div>
        <% end %>

  </div>
  <%= link_to 'Edit', edit_store_path(@store), class: 'btn btn-primary' %> |
  <%= link_to 'Back', stores_path, class: 'btn btn-primary' %>|
  <%= link_to 'New Product', new_store_product_path(@store), class: 'btn btn-secondary' %>

</div>
